version: 2
project_name: fundamentus-fii-pp-cli
changelog:
  disable: true
builds:
  - id: fundamentus-fii-pp-cli
    main: ./cmd/fundamentus-fii-pp-cli
    binary: fundamentus-fii-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X fundamentus-fii-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: fundamentus-fii-pp-mcp
    main: ./cmd/fundamentus-fii-pp-mcp
    binary: fundamentus-fii-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: fundamentus-fii-pp-cli
    repository:
      owner: 
      name: homebrew-tap
    homepage: "https://github.com//fundamentus-fii-pp-cli"
    description: "Fundos Imobiliários do Fundamentus via Crawl4AI — dados estruturados: cotação, DY, P/VP, vacância, segmento, etc."
    install: |
      bin.install "fundamentus-fii-pp-cli"
      bin.install "fundamentus-fii-pp-mcp"
